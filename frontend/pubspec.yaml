name: mitroo_frontend
description: Mitroo – organisation asset management & team dispatch UI
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  provider: ^6.1.2
  go_router: ^14.8.1
  shared_preferences: ^2.3.4
  intl: ^0.19.0
  google_fonts: ^6.2.1
  mobile_scanner: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
